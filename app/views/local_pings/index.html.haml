%h1 Лог локальных пингов

%hr

.well
  %table.table.table-striped.pretty
    %thead
      %tr
        %th ip
        %th mac
        %th up
        %th down
    %tbody
      - if @logs.length > 0
        - @logs.each do |log|
          %tr
            %td= link_to log.ip, local_ping_path(log.ip)
            %td= log.mac
            %td= log.up.utc.strftime("%H:%M:%S, %d %b %Y")
            %td
              - if log.down
                = log.down.utc.strftime("%H:%M:%S, %d %b %Y")
      - else
        %tr
          %td{:colspan => 4}
            Записи отсутствуют

- content_for :sidebar, render(:partial => 'sidebar', :locals => { :action_url => local_pings_path })
- content_for :sidebar, render(:partial => 'networks')
- content_for :sidebar, render(:partial => 'clear_logs')

= will_paginate @logs, :class=>"pagination pagination-centered"