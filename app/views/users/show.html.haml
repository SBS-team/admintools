%h1 User info

-if @user
  .well
    %p= "#{@user.first_name} #{@user.last_name}"
    %p= @user.email
    %p= @user.skype
    %p= @user.info
    = link_to 'Edit', edit_user_path(@user.id), :class => 'btn btn-inverse'
    = link_to 'Back', users_path, :class => 'btn btn-info'
