= "Запрос касательно расширения временного лимита на период #{@time_request.request_date} - #{@time_request.request_date.end_of_month} #{@time_request.approved ? @old_time.nil? ? "принят" : "изменен с #{@old_time} на #{@time_request.requested_time}" : "отклонен"}"