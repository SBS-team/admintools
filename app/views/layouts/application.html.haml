!!!
%html{:lang => "en"}
  %head
    %meta{:charset => "utf-8"}
      %meta{:content => "IE=Edge,chrome=1", "http-equiv" => "X-UA-Compatible"}
        %meta{:content => "width=device-width, initial-scale=1.0", :name => "viewport"}
          %title= content_for?(:title) ? yield(:title) : "Admintools"
          = csrf_meta_tags
          = yield :calendar
          / Le HTML5 shim, for IE6-8 support of HTML elements
          /[if lt IE 9]
            <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
          <script src="/nicEdit/nicEdit.js"></script>
          = stylesheet_link_tag "application", :media => "all"

  %body
    .wrapper
      .navbar.navbar-fixed-top
        .navbar-inner
          .container-fluid
            %a.btn.btn-navbar{"data-target" => ".nav-collapse", "data-toggle" => "collapse"}
              %span.icon-bar
              %span.icon-bar
              %span.icon-bar
            %a.brand{:href => "#"} Admintools
            .container-fluid.nav-collapse{:style => "float: right"}
              = link_to "Администраторы", admins_path, :class => "btn btn-large btn-primary", :id => "admins_tab"
              - if admin_signed_in?
                Signed in as
                %strong
                  #{current_admin.name}
                = link_to "Sign out", destroy_admin_session_path, :method => :delete, :class => "btn btn-danger"
      .container-fluid
        .row-fluid
          .span9
            %ul.breadcrumb
            %ul.nav.nav-tabs
              %li#rooms_tab= link_to "Офисы", rooms_path
              %li#desktops_tab= link_to "Компьютеры", desktops_path
              %li#users_tab= link_to "Пользователи", users_path
              %li#devices_tab= link_to "Устройства", devices_path
              %li#users_tab= link_to "Календарь", calendar_path
            - if notice
              .alert.alert-info
                %a.close{"data-dismiss" => "alert", :href => "#"}
                  ×
                = notice
            - if alert
              .alert.alert-alert
                %a.close{"data-dismiss" => "alert", :href => "#"}
                  ×
                = alert
            = yield
          .span3
            = yield :sidebar
      .push
    %footer.container-fluid
      %p © Company 2012
    = javascript_include_tag "application"